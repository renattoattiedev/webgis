# Frontend React — variveis de ambiente (desenvolvimento)
# Aponta para o backend NestJS via proxy do Vite (/api -> localhost:3333)
# Secret token usado no fluxo de autenticação por plugin (dev local)
VITE_API_URL=/api
VITE_PLUGIN_SECRET=c48afe16-b76a-4429-b2d2-0e370747d9b7